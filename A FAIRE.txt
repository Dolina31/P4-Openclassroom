ACCUEIL :
- crée une boucle pour le loader
- regler le problème du loader qui est trop "sec" au démarrage
- enlever les espaces html
- enlever temporairement lo loader penser à le remettre
    .loader-container {
    position: fixed;
    width: 100%;
    height: 100%;
    z-index: 1;
    background: linear-gradient(25deg, rgba(255,121,218,1) 0%, rgba(165,110,232,1) 100%);
    animation: loader-fade-out 1s 3s forwards;

    .loader {
        width: 100%;
        height: 100%;
        color: white;
        @include flex-position (null, center, center);
    }

    .loader__logo {
        font-size: 40px;
        font-family:'Shrikhand', cursive;
        padding: 5px;
    }

}

PAGE MENU :
- ranger les imbrication pour qu'elles soient plus lisible et déplacer @keyframes
- Chercher pourquoi la police d'écriture dans les boutons en font-weight 300 n'a pas la même apparence que celle de l'accueil
